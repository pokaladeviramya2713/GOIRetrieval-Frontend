<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GOIretrieval" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/light_blue</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
    <style name="Theme.GOIretrieval" parent="Base.Theme.GOIretrieval" />

    <!-- Custom Style for Dropdown Popup -->
    <style name="CustomDropdownStyle" parent="Widget.MaterialComponents.AutoCompleteTextView.OutlinedBox">
        <item name="android:popupBackground">@android:color/white</item>
        <item name="android:textColor">@color/navy_home</item>
        <item name="android:paddingHorizontal">16dp</item>
    </style>
</resources>